#!/usr/bin/env bash
# Above line is the modern sheBang for production rather that just specifying exact path like /bin/bash (Can fail if bash is on diff path) . For maximum portable sheBang we use env file to find bash & executes it
