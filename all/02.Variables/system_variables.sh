#!/bin/bash

# System Defined Variables are mostly in UpperCase
# $ sign is used before variable name to access the value inside it

echo $HOME
echo $PWD
echo $BASH
echo $BASH_VERSION
echo $OSTYPE # OS Name
echo $LOGNAME # UserName

# To check all system_defined variables run `env` or `set` etc in terminal
